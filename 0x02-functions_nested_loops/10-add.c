#include "main.h"

/**
 * add - Adds two integers and returns the result.
 * @a: first int to be added (a)
 * @b: second int to be added (b)
 * Return: Sum of int(a) and int(b)
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
